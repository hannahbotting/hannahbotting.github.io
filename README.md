# My Personal Website

This repository is contains the source for my personal website

## Development

1. Install dependencies with `npm install`.
2. Run `npm run dev`.
3. Build with `npm run build`.

The Vite config uses `base: './'` so the build can be deployed from GitHub Pages.